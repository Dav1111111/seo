"""Draft Profile Builder (Phase F).

Auto-generates a `target_config`-shaped proposal from crawled pages and
observed queries, then writes it to `sites.target_config_draft`.

See `builder.build_draft_profile` for the orchestrator entry point.
"""
