"""Media / publisher vertical — stub (news, magazines, blogs).

Not implemented. Sites with vertical='media' will fall back to
tourism/tour_operator via registry default until this profile is populated.

TODO: intent_rules (news queries — breaking/trend/topic, how-to, explainer),
entity patterns (people/orgs/events from knowledge graph), page_requirements
(byline, publish date, update date, TOC), schema_rules (NewsArticle, Article,
Person, FAQPage), commercial_factors (ads balance, paywall UX, subscription CTA).
"""

from __future__ import annotations

IMPLEMENTED = False
